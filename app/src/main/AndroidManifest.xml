<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MamaMindTrial"
        tools:targetApi="31">

        <!-- Specify MainActivity as the launcher -->
        <activity
            android:name=".NextSignUp"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <activity
            android:name=".HomePage"
            android:exported="false" />

        <activity
            android:name=".Mamamind_login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities in your application -->
        <activity android:name=".Mamamind_teaser" android:exported="false" />
        <activity android:name=".Mammind_m" android:exported="false" />
<!--        <activity android:name=".Mamamind_login" android:exported="false" />-->
        <activity android:name=".mamamind" android:exported="false" />


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
<!--            android:name=".Chp_profile"-->
<!--            android:exported="false" />-->

        <activity
            android:name=".SignUp"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
